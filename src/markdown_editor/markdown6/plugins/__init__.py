"""Plugin system for the Markdown editor.

Public surface lives in `markdown_editor.plugins` (the top-level shim
module). This inner package contains implementation details: metadata
parsing, discovery, registration wiring, and the document handle.
"""
